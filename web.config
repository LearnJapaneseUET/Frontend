<configuration>
  <system.webServer>
    <rewrite>
      <rules>
        <rule name="Rewrite all requests" stopProcessing="true">
          <match url=".*" />
          <action type="Rewrite" url="/" />
        </rule>
      </rules>
    </rewrite>
  </system.webServer>
</configuration>
